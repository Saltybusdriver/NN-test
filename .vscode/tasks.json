{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make-debug",
            "type": "shell",
            "command": "make clean && make DEBUG=1",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}